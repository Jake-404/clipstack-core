"""CrewAI crews per Doc 1 §7.x and Doc 4 §2.6–§2.10.

Phase A.0 ships only `content_factory`. The roadmap is enumerated in
`main.list_crews()` so the surface stays discoverable even before
implementations land.
"""
